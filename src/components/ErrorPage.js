import React, { PropTypes } from 'react';
import Button from '@material-ui/core/Button'

const Error404 = props => {
    return (
        <div>
            <p> ERROR 404 </p>
                RETUNR TO <Button>HOME</Button>
        </div>
    );
};

Error404.propTypes = {
    
};

export default Error404;